<?php

function nzfoverzicht_menu() {
	$items = array();

	$items['nzfoverzicht'] = array(
		'title' => t('NZF Overzicht'),
		'page callback' => 'nzfoverzicht_page',
		'access callback' => 'user_is_logged_in',
		'description' => t('NZF Overzicht'),
		'type' => MENU_CALLBACK,
    'file' => 'nzfoverzicht.inc',
	);

	return $items;
}

